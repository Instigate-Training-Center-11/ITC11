delimiter //

create trigger t after update on order_list
for each row
	begin
	if orders.isbool = 1 then
		update orders set orders.time = NOW() where orders.order_id = new.order_id;
	END IF;
	END //

delimiter ;
