delimiter //
create procedure p (aa int, bb int)
	begin
		select * from customers inner join orders using(customers_id)
		inner join order_list using(order_id)
		inner join product using(product_id) where customers_id = aa and product_id = bb;
	END//
delimiter ;

call p(1, 2);
