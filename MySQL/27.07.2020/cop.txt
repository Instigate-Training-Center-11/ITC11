1. -->  select c_name, c_surname, p_name, p_brand, p_quantity from customers inner join orders on customers.c_id = orders.c_id inner join orderLines on orders.o_id = orderLines.o_id inner join products on orderLines.p_id = products.p_id where customers.c_id = '2';

2. -->  select o_delivery_time from orders where o_id = 1;

3. -->  select count(o_completed) from orders where o_completed = 0;

4. -->  select sum(quantity_in_stock) from products;

5. --> select p_name, p_brand, s_name from suppliers inner join products on suppliers.s_id = products.s_id where products.p_id = '1';

6. -->  create temporary table for_total_cost select o_id, p_name, p_brand, p_quantity, p_unit_sale_price from orderLines inner join products on orderLines.p_id = products.p_id where orderLines.o_id = '1';
ALTER TABLE for_total_cost ADD COLUMN total_cost int as (p_quantity * p_unit_sale_price);
select sum(total_cost) from for_total_cost;

